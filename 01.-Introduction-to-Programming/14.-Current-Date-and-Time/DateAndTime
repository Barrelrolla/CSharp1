using System;
// Задача 14. Create a console application that prints the current date and time.

    class DateAndTime
    {
        static void Main()
        {
        Console.WriteLine(DateTime.Now);
        }
    }
